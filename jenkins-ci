pipeline {
    agent {
      label 'aws-agent'
    }
    stages {
        stage('build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('test') {
            steps {
                sh 'mvn test'
            }
        }
    } // closes stages
} // closes pipeline

