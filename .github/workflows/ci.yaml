name: ci workflow
on:
  push:
    branches:
      - "main"
jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - name: checkout step
        uses: actions/checkout@v5
      
      - name: build
        run: mvn clean package
        shell: bash
